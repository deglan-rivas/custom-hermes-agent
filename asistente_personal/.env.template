# Copy this file to `.env` and fill in real values, then `chmod 600 .env`.
# See ../openspec/changes/hermes-personal-assistant/proposal.md §4 (Fase 0 checklist)
# for where each value comes from. Never commit `.env` — it is gitignored.

# --- LLM endpoint (F0.1 - CONFIRMED: opencode Go + deepseek-v4-pro) ---
LLM_BASE_URL=https://opencode.ai/zen/go/v1
LLM_API_KEY=

# --- Telegram (F0.3 allowlist mechanism CONFIRMED, F0.4 token/user_id pending) ---
TELEGRAM_TOKEN=
TG_USER_ID=

# --- Hermes state volume (D9 - leave default unless doing the laptop restore drill) ---
HERMES_DATA=./state/hermes

# --- Backup (F0.5 - OPEN, hard blocker. Do not fill until F0.5 is closed) ---
RESTIC_REPOSITORY=
RESTIC_PASSWORD=
RCLONE_REMOTE=
