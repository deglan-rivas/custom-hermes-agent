# Least-privilege watchdog credentials (D6 -- design.md §5 D6, Group 6 task 6.2).
#
# Copy to `ops/.env.ops`, fill in real values, then `chmod 600 ops/.env.ops`. Sourced
# ONLY by host-level scripts (check-stack.sh, check-backup.sh, notify.sh,
# skills-autocommit.sh) -- they must never see LLM_API_KEY or RESTIC_* from the main
# `.env`. A script that runs unattended every 15 minutes should not have credentials
# it doesn't need.

TELEGRAM_TOKEN=
TG_USER_ID=

# Set to 1 if F0.2 (GPU passthrough) stays unresolved and `whisper` is intentionally
# not brought up -- check-stack.sh (Group 7) then does not expect the `whisper`
# service to be running/healthy (design §10, spec §4 "GPU unavailable degrades
# gracefully").
WHISPER_OPTIONAL=0
