# Group 7, task 7.8 `[BLOCKED: F0.5]` (design.md §10, D12). ALTERNATIVE to
# /etc/cron.d/hermes-ops -- install only ONE of the two mechanisms. See
# ops/check-backup.sh header: blocked until F0.5 closes and the restic
# service produces real snapshots (task 7.9).

[Unit]
Description=Hermes ops: restic backup freshness check
After=docker.service

[Service]
Type=oneshot
WorkingDirectory=/srv/asistente_personal
ExecStart=/srv/asistente_personal/ops/check-backup.sh
